# Hugo build output
/public/
/resources/_gen/
/.hugo_build.lock

# Reference material — unlicensed third-party source, kept locally for behavioral reference only
/docs/

# Process artifacts — brainstorm specs/plans, not final deliverables
/docs/superpowers/

# OS cruft
.DS_Store
Thumbs.db

# Claude Code local state (machine-specific permission grants, etc.)
# .claude/launch.json itself IS tracked — this only excludes local-only files
/.claude/settings.local.json
